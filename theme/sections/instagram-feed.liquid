<section class="ig-feed reveal" data-testid="instagram-feed">
  <div class="container">
    <div class="section-head">
      <div class="eyebrow">{{ section.settings.handle | default: '@snagnest' }}</div>
      <h2>{{ section.settings.heading | default: 'Trending finds, <em>curated for you</em>' }}</h2>
      {% if section.settings.subheading != blank %}<p>{{ section.settings.subheading }}</p>{% endif %}
    </div>
    <div class="ig-grid">
      {% for block in section.blocks %}
        <a href="{{ section.settings.profile_url | default: 'https://instagram.com/snagnest' }}" target="_blank" rel="noopener noreferrer" class="ig-tile" {{ block.shopify_attributes }} data-testid="ig-tile-{{ forloop.index0 }}">
          {% if block.settings.image != blank %}
            <img src="{{ block.settings.image | image_url: width: 600 }}" alt="{{ block.settings.caption | escape }}" loading="lazy" />
          {% elsif block.settings.fallback_url != blank %}
            <img src="{{ block.settings.fallback_url }}" alt="{{ block.settings.caption | escape }}" loading="lazy" />
          {% endif %}
          <div class="ig-overlay">📸 {{ block.settings.caption }}</div>
        </a>
      {% endfor %}
    </div>
    <div class="ig-foot">
      <a href="{{ section.settings.profile_url | default: 'https://instagram.com/snagnest' }}" target="_blank" rel="noopener noreferrer" class="btn btn-secondary" data-testid="follow-instagram-btn">Follow on Instagram</a>
    </div>
  </div>
</section>

{% stylesheet %}
.ig-feed { padding: 5rem 0; background: var(--color-cream); }
.ig-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: .75rem; }
.ig-tile { position: relative; aspect-ratio: 1; border-radius: 18px; overflow: hidden; display: block; cursor: pointer; transition: transform .25s ease; }
.ig-tile:hover { transform: translateY(-4px); }
.ig-tile img { width: 100%; height: 100%; object-fit: cover; transition: transform .4s ease; }
.ig-tile:hover img { transform: scale(1.06); }
.ig-overlay { position: absolute; inset: 0; background: linear-gradient(to top, rgba(31,41,55,0.85), rgba(31,41,55,0)); opacity: 0; transition: opacity .25s ease; display: flex; align-items: flex-end; padding: .75rem; color: #fff; font-size: .75rem; font-family: var(--font-heading); font-weight: 600; }
.ig-tile:hover .ig-overlay { opacity: 1; }
.ig-foot { text-align: center; margin-top: 2rem; }
{% endstylesheet %}

{% schema %}
{
  "name": "Instagram Feed",
  "settings": [
    { "type": "text", "id": "handle", "label": "Handle (eyebrow)", "default": "@snagnest" },
    { "type": "text", "id": "heading", "label": "Heading (HTML allowed)", "default": "Trending finds, <em>curated for you</em>" },
    { "type": "text", "id": "subheading", "label": "Subheading", "default": "Tag #snagnest for a chance to be featured." },
    { "type": "url", "id": "profile_url", "label": "Instagram profile URL" }
  ],
  "blocks": [
    {
      "type": "post",
      "name": "Instagram Post",
      "settings": [
        { "type": "image_picker", "id": "image", "label": "Image" },
        { "type": "url", "id": "fallback_url", "label": "Fallback image URL (optional)" },
        { "type": "text", "id": "caption", "label": "Caption", "default": "A find we love this week" }
      ]
    }
  ],
  "max_blocks": 12,
  "presets": [{
    "name": "Instagram Feed",
    "blocks": [
      { "type": "post", "settings": { "fallback_url": "https://customer-assets.emergentagent.com/job_6d3e1c7b-f001-4e06-8ed8-3843020b086b/artifacts/yvrv5cn8_IMG_9662.jpg", "caption": "This one surprised us (in the best way)" } },
      { "type": "post", "settings": { "fallback_url": "https://customer-assets.emergentagent.com/job_6d3e1c7b-f001-4e06-8ed8-3843020b086b/artifacts/g2hrd7bt_IMG_9494.jpg", "caption": "Cozy upgrade, approved" } },
      { "type": "post", "settings": { "fallback_url": "https://customer-assets.emergentagent.com/job_6d3e1c7b-f001-4e06-8ed8-3843020b086b/artifacts/q86lwpgi_IMG_1473.jpg", "caption": "The find of the month" } },
      { "type": "post", "settings": { "fallback_url": "https://customer-assets.emergentagent.com/job_6d3e1c7b-f001-4e06-8ed8-3843020b086b/artifacts/yvrv5cn8_IMG_9662.jpg", "caption": "Adventure mode: activated" } },
      { "type": "post", "settings": { "fallback_url": "https://customer-assets.emergentagent.com/job_6d3e1c7b-f001-4e06-8ed8-3843020b086b/artifacts/g2hrd7bt_IMG_9494.jpg", "caption": "Trending right now" } },
      { "type": "post", "settings": { "fallback_url": "https://customer-assets.emergentagent.com/job_6d3e1c7b-f001-4e06-8ed8-3843020b086b/artifacts/q86lwpgi_IMG_1473.jpg", "caption": "Simple things, big difference" } }
    ]
  }]
}
{% endschema %}
